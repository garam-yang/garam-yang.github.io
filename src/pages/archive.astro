---
import ArchivePanel from "@components/ArchivePanel.svelte";
import I18nKey from "@i18n/i18nKey";
import { i18n } from "@i18n/translation";
import MainGridLayout from "@layouts/MainGridLayout.astro";
import { getSortedPostsList } from "../utils/content-utils";
import { getTagList, getCategoryList } from "@utils/content-utils";
const sortedPostsList = (await getSortedPostsList()).map((post) => ({
	...post,
	data: {
		...post.data,
		category: post.data.category === null ? undefined : post.data.category,
	},
}));
---

<MainGridLayout title={i18n(I18nKey.archive)}>
<ArchivePanel
  sortedPosts={sortedPostsList.map((p) => ({
    ...p,
    data: { ...p.data, category: p.data.category ?? undefined },
  }))}
  tags={[]} 
  categories={[]}
  client:only="svelte"
/></MainGridLayout>

